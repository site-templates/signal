# Column types for posts.json — the source of truth the builder
# reads: richtext columns open in the rich editor (Content drawer
# and any section repeater listing this collection).
fields:
    title: { type: text }
    description: { type: textarea }
    date: { type: text }
    readTime: { type: text }
    author: { type: text }
    authorImage: { type: image }
    image: { type: image }
    imageAlt: { type: text }
    link: { type: url }
    slug: { type: text }
    descriptionDetail: { type: textarea }
    category: { type: text }
    readTimeDetail: { type: text }
    content: { type: richtext }
